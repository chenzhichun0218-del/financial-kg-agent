"""生成股权穿透专用 Obsidian 知识图谱"""
import os, sys
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from equity_penetration import EquityGraph

BASE = os.path.join(os.path.dirname(os.path.abspath(__file__)), "obsidian-kb-equity")
os.makedirs(BASE, exist_ok=True)

def w(name, content):
    with open(os.path.join(BASE, name), 'w', encoding='utf-8') as f:
        f.write(content)

print("Loading equity graph...")
g = EquityGraph()

# Collect 5 real cases
stocks = ['688765.SH', '603439.SH', '300838.SZ', '600993.SH', '600079.SH']
cases = []
for s in stocks:
    ctrls = g.find_controller(s, max_depth=3)
    flags = g.check_flags(s)
    top = sorted(g.reverse.get(s, {}).items(), key=lambda x: x[1]['pct'], reverse=True)[:5]
    cases.append({
        'stock': s, 'controllers': ctrls, 'flags': flags.get('flags', []),
        'top': [(h, i['pct'], i['type']) for h, i in top],
        'total': flags.get('total_known_holdings', 0),
        'max_d': max((c['depth'] for c in ctrls), default=0),
        'multi': any(c['depth'] >= 2 for c in ctrls),
    })

# ============ MOC ============
moc = """---
tags: [MOC, 股权穿透]
---

# 股权穿透知识图谱

> 从上市公司出发，沿着持股关系一层层追溯，直到挖出最终的实际控制人。

## 概念入门
- [[什么是股权穿透]] — 大白话解释
- [[为什么要做穿透]] — 对散户有什么用
- [[穿透的难点]] — 层数深、交叉持股、代持、名称不统一

## 技术实现
- [[图数据结构]] — 70,221 节点, 118,565 条边, 两个方向
- [[BFS多跳算法]] — 逐层追溯 + visited去重
- [[有效持股计算]] — 累乘公式
- [[名称匹配]] — 3,388 条公司名映射
- [[金融中介过滤]] — 过滤掉47%的银行/基金/券商

## 风险信号
- [[股权风险信号]] — 分散、无实控、一股独大、控制链长

## 真实案例
"""
for i, c in enumerate(cases, 1):
    tag = "多层穿透" if c['multi'] else "直接持股"
    moc += f"- [[案例{i}-{c['stock']}]] — {tag}\n"

w("00-股权穿透知识图谱.md", moc)

# ============ 概念笔记 ============
w("什么是股权穿透.md", """---
tags: [概念]
---

# 什么是股权穿透

## 大白话

股票软件显示"大股东: 上海力鸿, 持股50.1%"。你以为上海力鸿是老板。

但上海力鸿背后还有人 → **穿透就是顺着持股关系往上挖，一直挖到人。**

## 三层

| 层 | 查什么 | 例子 |
|-----|------|------|
| 表层 | 直接持股 | 上海力鸿 → 九阳股份 |
| 中层 | 壳公司背后 | 张某某 → 上海力鸿 |
| 底层 | 实际控制人 | 张某某(自然人) |

## 和十大股东的区别

十大股东 = 1层名单。穿透 = N层链路。

## 关联
- [[为什么要做穿透]]
- [[BFS多跳算法]]
""")

w("为什么要做穿透.md", """---
tags: [概念]
---

# 为什么要做穿透

## 三个场景

1. **避开"掏空型"老板**: 穿透发现实控人同时控制8家上市公司, 3家已ST → 远离

2. **国资vs个人**: 国资委实控 → 稳。个人实控+持股已质押95% → 爆雷风险

3. **提前发现跑路**: 公告显示"减持2%", 穿透发现实控人关联方累计已减持15%

## 关联
- [[什么是股权穿透]]
- [[股权风险信号]]
""")

w("穿透的难点.md", """---
tags: [概念]
---

# 穿透的难点

## 四道坎

1. **层数深**: 实控人藏在5-6层壳公司后面
2. **交叉持股**: A→B→C→A 成环, BFS会死循环 → 需要[[BFS多跳算法|visited去重]]
3. **代持**: 明面张三, 实际李四 → 数据里完全看不出来
4. **名称不统一**: "深圳腾讯" vs "深圳市腾讯计算机系统有限公司" → 需要[[名称匹配]]

## 关联
- [[什么是股权穿透]]
- [[名称匹配]]
""")

# ============ 技术笔记 ============
w("图数据结构.md", """---
tags: [技术]
---

# 图数据结构

## 规模

```
节点: 70,221 实体
边:   118,565 持股关系
过滤: 79,224 金融中介
```

## 双向图

**正向** `forward[holder] = {target: {pct, type}}`
→ 查"杨代常控制了哪些公司"

**反向** `reverse[stock] = {holder: {pct, type}}`
→ 查"688765被谁持有"

## 节点类型

| 类型 | 标识 | 示例 |
|------|------|------|
| 个人 | type=个人 | 杨代常、张海 |
| 企业 | type=企业 | 人福医药、上海同盛 |
| 中介 | 已过滤 | 香港中央结算、社保基金 |

## 关联
- [[BFS多跳算法]]
- [[金融中介过滤]]
""")

w("BFS多跳算法.md", """---
tags: [技术, 算法]
---

# BFS多跳算法

## 为什么BFS

BFS逐层展开 → 先看最近的控制人 → 再看背后的控制人。
DFS一条路走到黑不适合。

## 伪代码

```
queue = [(起始股票, 路径, 持股链)]
visited = set()

while queue:
    当前, 路径, 持股链 = queue.pop()
    if 深度 >= 4: continue
    if 当前 in visited: continue  # 去重防环
    visited.add(当前)

    for 股东, 比例 in 反向图[当前]:
        记录: (新路径, 深度, 有效持股)
        if 股东是"企业": 继续追溯
        else: 到达终点(个人)
```

## 去重

交叉持股A→B→C→A会形成环。`visited` 集合记录已访问节点, 遇到直接跳过。

## 关联
- [[图数据结构]]
- [[有效持股计算]]
- [[名称匹配]]
""")

w("有效持股计算.md", """---
tags: [技术]
---

# 有效持股计算

## 公式

```
有效持股 = 第1层% × 第2层% × ... × 第N层%
```

## 示例

```
张三 → (80%) A公司 → (60%) B基金 → (50.1%) 上市公司
有效 = 0.80 × 0.60 × 0.501 = 24.05%
```

张三直接持股0%, 但穿透后实际控制24.05%。

## 为什么越穿透越少

每过一层都在稀释 → 第3层只有24% → 可能被其他股东联合推翻 → 见[[股权风险信号]]

## 关联
- [[BFS多跳算法]]
""")

w("名称匹配.md", """---
tags: [技术]
---

# 名称匹配

## 问题

"深圳腾讯" vs "深圳市腾讯计算机系统有限公司" → 同一家但字符串不同

## 方案

1. 从55,214篇研报提取 公司名→股票代码 映射
2. 去掉"股份有限公司""有限责任公司"等后缀
3. 取前4字符模糊匹配

## 局限

只能匹配3,388家已上市公司。非上市壳公司无法匹配。

## 关联
- [[穿透的难点]]
""")

w("金融中介过滤.md", """---
tags: [技术, 数据清洗]
---

# 金融中介过滤

## 为什么

香港中央结算持有3,166只A股 → 但不控制任何一家（只是代持）。

## 过滤对象

港股通代持、国家队、社保、指数基金、QFII、券商自营、公募基金...

## 效果

149,445 → 70,221 实体 (去掉47%)

## 关联
- [[图数据结构]]
""")

# ============ 风险 ============
w("股权风险信号.md", """---
tags: [风险]
---

# 股权风险信号

## 四大预警

| 信号 | 阈值 | 风险 |
|------|------|------|
| 股权分散 | 前十大<30% | 易被举牌收购 |
| 无自然人实控 | 追溯3层找不到 | 国资/外资/刻意隐藏 |
| 一股独大 | 第一大>70% | 缺乏制衡, 可掏空公司 |
| 控制链过长 | >=3层 | 隐性关联交易 |

## 关联
- [[为什么要做穿透]]
- [[有效持股计算]]
""")

# ============ 案例 ============
for i, c in enumerate(cases, 1):
    note = f"""---
tags: [案例]
stock: {c['stock']}
depth: {c['max_d']}
---

# 案例{i}: {c['stock']}

{'**多层穿透案例**' if c['multi'] else '**直接持股案例**'}

## 前5大股东 (合计{c['total']:.1f}%)

| 股东 | 持股 | 类型 |
|------|------|------|
"""
    for h, p, t in c['top']:
        note += f"| {h[:25]} | {p:.1f}% | {t} |\n"

    note += "\n## 控制链路\n\n"
    for j, ctrl in enumerate(c['controllers'][:5], 1):
        tag = "[个人]" if ctrl['controller_type'] == '个人' else "[企业]"
        note += f"{j}. {tag} {ctrl['chain']}\n"
        note += f"   - 有效持股: {ctrl['effective_pct']:.2f}%\n"
        note += f"   - 深度: {ctrl['depth']} 层\n\n"

    if c['flags']:
        note += "## 股权风险\n\n"
        for fl in c['flags']:
            note += f"- ! {fl['flag']}: {fl['detail']}\n"

    if c['multi']:
        note += """
## 穿透说明

多层穿透的过程：
1. [[图数据结构|反向图]]查第一层股东
2. 企业股东通过[[名称匹配]]转股票代码
3. [[BFS多跳算法|BFS]]继续查上面的股东
4. [[有效持股计算|累乘]]得出有效持股比例
"""

    note += "\n## 关联\n- [[00-股权穿透知识图谱]]\n- [[什么是股权穿透]]\n- [[BFS多跳算法]]"
    w(f"案例{i}-{c['stock']}.md", note)

# ============ 应用 ============
w("正向挖掘资本版图.md", """---
tags: [应用]
---

# 正向挖掘资本版图

## 做什么

输入人名/公司名 → 输出他控制的全部上市公司

## 方法

```python
direct = forward[name]  # 直接持股
for target in direct:
    if target.type == "企业":
        indirect = forward[target]  # 间接持股
```

## 关联
- [[图数据结构]]
- [[什么是股权穿透]]
""")

print(f"Done! {len(os.listdir(BASE))} notes in obsidian-kb-equity/")
for f in sorted(os.listdir(BASE)):
    print(f"  {f}")
